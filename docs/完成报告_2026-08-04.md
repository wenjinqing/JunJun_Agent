# 完成报告：全功能测试 + 修复 + Skills 落地（2026-08-04）

> 对应需求：① 全部功能综合测试 + 测试文档 → ② 修复计划文档 + 按计划修复（重点 LLM 主动决策工具）
> → ③ 评估并落地 md 格式 skills → ④ 本报告。
> 提交：`7fde513`（测试+修复）、`125fafc`（skills + CLAUDE.md）。

## 一、测试（产出：《功能测试报告_2026-08-04.md》）

新建可重复执行的体检脚本 `scripts/functional_check.py`，五个维度：

| 维度 | 结果 |
|---|---|
| 环境配置矩阵（12 项） | 8 完整 / 4 缺 key（GSV2P、SoVITS、谷歌搜索、天气——均为可选后备） |
| 插件装载审计（27 目录） | 24 加载 + 1 配置禁用 + 2 遗留空壳（已清） |
| LLM 工具静态检查（55 个） | 全部有 docstring；唯一过短的 list_reminders 已修 |
| 只读工具真实调用（23 个） | **23/23 PASS**（首轮 4 个 FAIL 系探测脚本参数名写错，已修正复跑） |
| LLM 双槽活性 + pytest | utils/agent 槽均活；**1233 全绿** |

测试过程中揪出 2 个真 bug（同一根因——**测试自污染生产库**）：
`test_pending_hint_injected` 与 `test_w1w2_final` 把 stub 图片的 VLM 描述写进生产
Images 表，前者毒死自己（下轮缓存命中）、后者每轮累积垃圾行。均已改内存库
bind_ctx，生产污染行已删除。纪律已固化进 CLAUDE.md。

## 二、修复（产出：《修复计划_2026-08-04.md》，P0/P1 全部清零）

- **P0×2**：上述两个测试隔离修复（测试过程中就地完成）。
- **P1-3**：`list_reminders` docstring 14→79 字，补触发场景与返回说明。
- **P1-4**：55 个工具 docstring 全量审计「何时使用」要素——除 list_reminders 外全部合格；
  关键边界三人组已互相标注：**recall_memory**（聊天记忆/约定）vs
  **search_knowledge**（知识库资料/设定）vs **query_chat_history**（当前会话原文精确查找）。
- **P1-5**：删除遗留插件空壳 `pixiv_novel`、`lolicon_setu`（功能已并入 pixiv v2；
  均未入库，无密钥损失）。工具装载数 55 不变。
- **P2（建议未动代码）**：补谷歌搜索 key 防 web_search 单点；两个「历史上的今天」暂保留。

回归：全套件 1233 绿、工具 55/55、过短 docstring 清零。

## 三、Skills（评估结论：需要，已落地 5 个 + CLAUDE.md）

**评估**：仓库此前没有任何 `.claude/skills`，连 CLAUDE.md 都没有——每次会话都靠
重新摸索。本次会话暴露的重复痛点（测试污染生产库、探测参数误报、重启流程、
人设复读机制）正好对应可复用工作流，因此落地：

| Skill | 触发场景 | 内容要点 |
|---|---|---|
| `functional-check` | 全面测试/体检 | 体检脚本用法、SKIP/DEGRADED 解读纪律、FAIL 先怀疑探测脚本、报告与修复计划模板 |
| `plugin-dev` | 加插件/新工具 | 插件解剖（_manifest/probe_available/白名单/admin 门）、docstring 铁律（≥15字+触发场景+边界互注）、装载验证 |
| `restart-deploy` | 重启/没反应 | 三件套顺序、什么改动重启什么、看门狗状态机（等扫码时不重启）、上线验证、排查顺序 |
| `prod-verify` | 线上验证/观察期 | 清单四段式、观察渠道、关闭开关先行、灰度路径（私聊→单群→全量）、本期四个观察指标 |
| `persona-tune` | 人设/口吻调整 | 调色盘方法论、四个配置落点、「prompt 里每句台词都会被复读」铁律、相关测试清单 |

外加 **`CLAUDE.md`**（仓库根，永远自动加载）：生产库只读、测试必须内存库
bind_ctx、密钥遮蔽、gitignore 清单、边做边 commit、回归基线——把两次生产库
污染事故的纪律钉死在每个会话的上下文里。

## 四、当前状态与下一步

- 代码侧：本轮需求全部完成，工作区干净（仅剩 gitignored 的配置备份与调色盘原文）。
- 生产侧：本次所有改动（含此前的人设 v3 / echo guard / send-retry / 情绪修复）
  **尚未重启生效**——按路线图进入 Phase 0 验证周：重启三件套
  （`/restart-deploy`）→ 对照生产验证清单验收（`/prod-verify`）→ 观察 5 天。

## 五、补充（2026-08-04 下午）：君君本人的 md skills + 媒体发送修复

用户澄清 skill 指的是**君君（Agent 本人）**的 md 技能包，已落地：

- **机制**（与 Claude Code skill 同模式）：手册放 `junjun_skills/agent_skills/<name>.md`
  （frontmatter 带 name/when）；system prompt 只注入索引（`<skills>` 块，
  名字+何时用）；模型命中场景时调新工具 `use_skill(name)` 取全文照做。
  加载器 `junjun_skills/skills_md.py`（mtime 缓存），索引注入在 persona。
- **关键防护**：`use_skill` 加入工具掩码 CORE 层（第 9 席）——否则它会被话题
  掩码裁掉，prompt 索引指向一个不存在的工具（「追问未绑定工具」式陷阱）。
- **首批 6 本手册**：video-watching（看视频全流程）/ deep-tasks（耗时派单）/
  drawing（画图）/ pixiv（找图发图）/ subscription（盯梢）/ search-facts
  （四个「查」的边界）。全部对齐现有 rules 与工具 docstring，写流程与分寸不写台词。
- 体检脚本 LIVE_PROBES 新增 use_skill 实调；测试 8 个（加载/索引/工具/注入）。
- **顺手修了生产报错**：NapCat「文件处理失败 识别URL失败」——bilibili 插件发视频
  传裸 Windows 路径，adapter 发端收口 `_media_file` 统一转 `file:///` URI
  （image/record/video 三类），commit `f5b7a9a`。
- 全套件 1242 绿（+9）。
