# 修复计划（2026-08-04，基于《功能测试报告_2026-08-04》）

> 范围原则：以 **LLM 主动决策的工具** 为主——docstring 是模型选工具的唯一依据，
> 优先级最高；测试基础设施问题次之；配置项只给建议不动代码。

## P0（已完成，测试过程中就地修复）

| # | 问题 | 修复 | 状态 |
|---|---|---|---|
| 1 | test_pending_hint_injected 自污染（写生产 Images 表毒死自己） | 改内存库 bind_ctx + 删生产污染行 | ✅ |
| 2 | test_w1w2_final 每轮向生产 Images 表写垃圾行 | 改内存库 bind_ctx | ✅ |

## P1（本次执行，LLM 决策工具相关）

| # | 问题 | 修复方案 | 验收 |
|---|---|---|---|
| 3 | list_reminders docstring 仅 14 字，模型不知道什么时候该调它 | 补全：何时用（对方问「我有什么提醒/你提醒我什么了」）+ 返回内容说明 | 静态检查 ≥15 字且含使用场景；pytest 绿 |
| 4 | 决策工具 docstring 全面体检 | 对 55 个工具 docstring 做「何时使用」要素审计，缺失的补齐（重点：与相邻工具的职责边界，如 recall_memory vs search_knowledge vs query_chat_history） | 每个工具 docstring 含触发场景 |
| 5 | 遗留插件空壳（pixiv_novel/lolicon_setu 只剩 __pycache__） | 删除目录 | 插件装载数不变、套件绿 |

## P2（建议，不动代码）

| # | 项 | 建议 |
|---|---|---|
| 6 | 谷歌搜索 key 缺失 | .env 补 GOOGLE_API_KEY/GOOGLE_CSE_ID，web_search 防单点 |
| 7 | GSV2P/SoVITS 后端 | 不用可忽略；要用按 docs/使用指南 配置 |
| 8 | 两个「历史上的今天」重叠 | 暂保留（命令入口不同）；下次插件整理时合并 |

## 执行顺序

P1-3 → P1-4 → P1-5 → 全套件回归 → 提交。
