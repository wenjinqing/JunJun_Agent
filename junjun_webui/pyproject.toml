[project]
name = "junjun-webui"
version = "0.1.0"
description = "WebUI"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["junjun_webui"]
