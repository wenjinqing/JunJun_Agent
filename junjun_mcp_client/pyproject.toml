[project]
name = "junjun-mcp-client"
version = "0.1.0"
description = "MCP ???"
requires-python = ">=3.11"
dependencies = [
    "mcp",
    "langchain-core"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["junjun_mcp_client"]
